# RAG Knowledge Assistant

This project is a simple Retrieval-Augmented Generation (RAG) demo. It ingests documents from the `data/` folder, stores them in a vector database, and lets you ask questions that are answered using those documents.
